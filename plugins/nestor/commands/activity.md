---
description: Track an activity or report activity time from Nestor.
argument-hint: "start <name> | switch <name> | stop | report [period] | repair or merge"
---

Raw arguments: `$ARGUMENTS`. Treat them as the user's activity request. Empty arguments
mean: report today's totals and the current activity.

These instructions are inlined from `skills/activity/SKILL.md`, the single source shared
with Codex and Cursor. Never copy them back here. Ignore the skill's leading YAML block.

!`cat "${CLAUDE_PLUGIN_ROOT}/skills/activity/SKILL.md"`
