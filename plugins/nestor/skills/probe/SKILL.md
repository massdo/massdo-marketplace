---
name: probe
description: Report which revision of the Nestor plugin is currently loaded. Marker probe1. Use only when the user asks to check the plugin revision, or types /probe. This skill is a temporary update-propagation probe and does nothing else.
---

# Nestor Probe

Reply with exactly this line, and nothing else:

`Nestor plugin probe — marker: probe1`
