interface:
  display_name: "Nestor Doctor"
  short_description: "Audit delivered tasks and propose closures — $nestor-beta:doctor [project-ref]"
  default_prompt: "Use $nestor-beta:doctor [project-ref] to audit merged deliveries and ask me to approve task closures; without a project ref, let me select one."

policy:
  allow_implicit_invocation: false
