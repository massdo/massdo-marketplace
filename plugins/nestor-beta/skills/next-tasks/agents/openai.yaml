interface:
  display_name: "Nestor Next Tasks"
  short_description: "List a project's active tasks — $nestor-beta:next-tasks [project]"
  default_prompt: "Use $nestor-beta:next-tasks [project] to list that project's active tasks; without a project name, infer it from this repository's Git remote."

policy:
  allow_implicit_invocation: false
