{
  "version": "0.9.0",
  "version_hash": "a18faeeeca56e4d4",
  "changelog": "Doctor now finds the project by itself: invoked without an argument, it reads the name from the workspace's Git repository instead of asking you to pick one. An explicit name still wins and is never checked against the repository."
}
