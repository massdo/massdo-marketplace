#!/bin/sh
# Every check this repository has, run before the commit lands. CI runs them
# after the push, which is too late for a direct commit on main.
# --baseline HEAD adds the one check CI cannot make on its own: a version bump
# that keeps the changelog of the previous release.
set -e
exec "$(git rev-parse --show-toplevel)/scripts/check.sh" --baseline HEAD
